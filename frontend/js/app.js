/**
 * App - Inicialização da aplicação.
 *
 * Carrega o header e renderiza a página inicial.
 */

document.addEventListener('DOMContentLoaded', function () {
  updateHeaderActions();
  navigateTo('home');
});
